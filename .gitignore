# Node.js dependencies - the node_modules folder is where all your installed npm packages are stored
# It should never be committed to the repository because it can be easily regenerated using `npm install`
/node_modules

# Build directory - this folder is created when you run `npm run build` to create a production build
# It contains the compiled and optimized code which is not necessary to be stored in version control
/build

# Log files generated by npm or yarn during the install process
# These files can contain debugging information or errors, but they are not needed in the repository
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE configuration files - these files are specific to your development environment (e.g., JetBrains IDEs or VSCode)
# They should not be included in the repository because they are specific to your local setup
.idea
.vscode

# OS-specific files - these are system files that are automatically generated by operating systems like macOS and Windows
# They are not relevant to your code and should not be committed to version control
.DS_Store
Thumbs.db

# Environment variables - files that store environment-specific settings like local development or production configurations
# These files should not be tracked as they often contain sensitive information (e.g., API keys)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor backup files - these are temporary files created by text editors like Vim or Emacs
# They store unsaved changes and should not be committed
*.swp
*.swo
